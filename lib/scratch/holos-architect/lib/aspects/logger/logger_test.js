"use strict";
module.exports = function (plugin) {
    var test = describe("'logger'' should have all its functions", function () {
        //it("Logger should ...", function testOn(item) {
        //    assert(false, "logger function failed...");
        //});
    });
    return test;
};