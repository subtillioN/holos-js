var assert = require("assert");
module.exports = describe("'env'' should have all its functions", function () {
    "use strict";
    it("Env should ...", function testOn (item) {
        assert(true,"env function failed...")
    });
});