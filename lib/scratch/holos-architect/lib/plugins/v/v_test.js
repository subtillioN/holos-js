"use strict";
var console = require("holodeck")(__filename);
module.exports = function (plugin) {
    var test = describe("'v'' should have all its functions", function () {
        //it("Enversion should ...", function testOn(item) {
        //    assert(false, "enversion function failed...");
        //});
    });
    return test;
};