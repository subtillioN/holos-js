var assert = require("assert");
module.exports = describe("'logger'' should have all its functions", function () {
    "use strict";
    //it("Logger should ...", function testOn (item) {
        //assert(false,"logger function failed...");
    //});
});