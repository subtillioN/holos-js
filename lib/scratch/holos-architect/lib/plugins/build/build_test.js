var assert = require("assert");
module.exports = describe("'build'' should have all its functions", function () {
    "use strict";
    it("Build should ...", function testOn (item) {
        assert(true,"build function failed...")
    });
});