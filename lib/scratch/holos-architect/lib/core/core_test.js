"use strict";
module.exports = function (plugin) {
    var test = describe("'core'' should have all its functions", function () {

        //it("Base should ...", function testOn(item) {
        //    assert(false, "core function failed...");
        //});
    });
    return test;
};